<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Alan Panayotov</title>
  <link rel="stylesheet" href="{{app_css}}">
</head>

<body>
  <header>
    <h1>Projects</h1>
  </header>
  {{>partials/nav}}
  <main class="projects">
    <article class="main">
      {{#each categories.main}}
        <a href="/projects/{{id}}">
          {{!-- {{../prefix}} {{firstname}} --}}
          <section style="background-image:url({{thumbnail}});"></section>
          <h2>{{title}}</h2>
        </a>
      {{/each}}
    </article>
    <article class="other">
      {{#each categories.other}}
        <a href="/projects/{{id}}">
          <h2>{{title}}</h2>
          <p>{{subtitle}}hello world</p>
        </a>
      {{/each}}
    </article>
  </main>
  {{>partials/footer}}
</body>

</html>