version: "3.8"
services:
  website:
    build: .
    ports:
      - "8081:8080"
    environment:
      - "DEV=true"
    volumes:
      - .:/website
      - type: bind
        source: ./.cargo/
        target: /root/.cargo/
