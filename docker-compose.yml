version: "3.8"
services:
  website:
    build: .
    ports:
      - "80:8080"
    environment:
      - "DEV=true"
    volumes:
      - .:/website
      - type: bind
        source: ./.cargo/
        target: /root/.cargo/
